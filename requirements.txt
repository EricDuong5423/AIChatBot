# Core Pipecat với OpenAI-compatible LLM (DeepSeek / Groq / Ollama / etc.)
pipecat-ai[openai]>=0.0.50

# HTTP server
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9       # FastAPI multipart/form-data (upload file)

# PDF text extraction (cho /docs/upload)
pypdf>=4.0.0

# Tiện ích
python-dotenv>=1.0.0
requests>=2.31.0

# RAG: LangChain + Supabase pgvector. Embedding qua Jina API (không cần local model).
# (Bỏ sentence-transformers + langchain-huggingface để giảm RAM ~700MB,
#  tránh kéo theo torch + nvidia-cuda-* — đây là lý do OOM trên Render free tier)
langchain>=0.3.0
langchain-community>=0.3.0
langchain-postgres>=0.0.12
langchain-text-splitters>=0.3.0
rank_bm25>=0.2.2              # hybrid retrieval (BM25 keyword + semantic)
psycopg[binary]>=3.1.0        # PostgreSQL driver cho pgvector (Supabase)

# URL crawl + HTML parse (cho /docs/crawl-url)
beautifulsoup4>=4.12.0
html2text>=2024.2.26

# Web search (fallback khi RAG không tìm thấy)
ddgs>=9.0.0

# OPTIONAL — chỉ cần khi chạy scripts/crawl.py local để re-crawl SPA sites.
# Không deploy lên Render (Playwright cần Chromium ~280MB).
# playwright>=1.45.0
